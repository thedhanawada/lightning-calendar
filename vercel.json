{
  "buildCommand": "ls -la && ls -la demo/ && cd demo && npm install && npm run build",
  "outputDirectory": "demo/dist",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}